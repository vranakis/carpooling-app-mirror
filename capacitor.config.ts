import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'gr.easyriderathens.app',
  appName: 'Easy Rider Athens',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
